# См. `.claude/AI.md`

Рабочая тетрадь нетривиальных находок проекта живёт в
`.claude/AI.md`. Этот файл намеренно вне git'а (см. `CLAUDE.md`).
На свежем `git clone` его ещё нет — заполняется по ходу работы,
по мере того как мы решаем задачи, чьи решения не очевидны при
чтении кода.
